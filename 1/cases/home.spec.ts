import { test, expect } from "@playwright/test";
import { getFile } from "@tests/core/config";

// test.use({ storageState: getFile("../.auth/applicant.json") });
// test("", async ({ page }) => {});
